/* eslint-disable no-undef */

const babelConfig = {
    plugins: ['@emotion'],
    presets: [
        '@babel/preset-env',
        '@babel/preset-react',
        '@babel/preset-typescript',
    ],
}

module.exports = babelConfig

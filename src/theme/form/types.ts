export type formType = {
    outline: string

    transition: string

    backgroundColor: string

    color: string
    padding: string

    fontSize: string
    fontFamily: string

    border: string
    borderFocus: string
    borderRadius: string
}

import styled from '@emotion/styled'

import { font, colors } from '@/theme'

export const ParagraphRoot = styled.p`
    color: ${colors.tertiary.grey};

    font-size: ${font.size[16]};
    line-height: ${font.lineHeight[16]};

    font-family: ${font.family};
`
